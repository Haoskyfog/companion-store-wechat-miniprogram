<!-- 员工端 - 我的老板 -->
<view class="page">
  <!-- 顶部导航 -->
  <view class="header">
    <view class="back-btn" bindtap="onBack">‹</view>
    <view class="header-title">我的老板</view>
  </view>

  <scroll-view scroll-y="{{true}}" class="scroll-content">
    <!-- 加载中 -->
    <view class="loading-state" wx:if="{{loading}}">
      <text class="loading-text">加载中...</text>
    </view>

    <!-- 空状态 -->
    <view class="empty-state" wx:elif="{{bossList.length === 0}}">
      <text class="empty-icon">👑</text>
      <text class="empty-text">暂无绑定的老板</text>
      <text class="empty-hint">请联系管理员进行绑定</text>
    </view>

    <!-- 老板列表 -->
    <view class="boss-list" wx:else>
      <view class="boss-card" wx:for="{{bossList}}" wx:key="_openid">
        <view class="boss-header">
          <view class="boss-avatar">
            <image wx:if="{{item.avatar}}" src="{{item.avatar}}" mode="aspectFill" class="avatar-image" />
            <text wx:else class="avatar-emoji">👑</text>
          </view>
          <view class="boss-info">
            <view class="boss-name-row">
              <text class="boss-name">{{item.nickname || '未设置昵称'}}</text>
              <view class="vip-badge" wx:if="{{item.vipLevel}}">{{item.vipLevel}}</view>
            </view>
            <text class="boss-id">ID: {{item.userId || '未设置'}}</text>
          </view>
        </view>
        <view class="boss-stats">
          <view class="stat-item">
            <text class="stat-label">钱包余额</text>
            <text class="stat-value balance">¥{{item.balance || 0}}</text>
          </view>
          <view class="stat-divider"></view>
          <view class="stat-item">
            <text class="stat-label">消费总额</text>
            <text class="stat-value consumption">¥{{item.totalConsumption || 0}}</text>
          </view>
        </view>
      </view>
    </view>

    <view class="bottom-spacer"></view>
  </scroll-view>
</view>
